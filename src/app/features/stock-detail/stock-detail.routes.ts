import { Routes } from '@angular/router';
import { StockDetailPageComponent } from './pages/stock-detail-page.component';

export const STOCK_DETAIL_ROUTES: Routes = [
  { path: '', component: StockDetailPageComponent },
];
